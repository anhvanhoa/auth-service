services:
  auth-service:
    build:
      context: .
      dockerfile: DockerFile
      secrets:
        - github_token
    image: auth-service:latest
    container_name: auth_service_app
    ports:
      - "40064:40064"
    networks:
      - sf_network
    environment:
      - NODE_ENV=production
      - CONFIG_FILE=${CONFIG_FILE}
    volumes:
      - ${CONFIG_FILE}:/config/auth_service.config.yaml
    restart: unless-stopped
    healthcheck:
      test:
        [
          "CMD",
          "wget",
          "--no-verbose",
          "--tries=1",
          "--spider",
          "http://localhost:40064/health",
        ]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

secrets:
  github_token:
    file: ./github_token.txt

networks:
  sf_network:
    external: true
